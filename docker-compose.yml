# Enterprise Insight RAG v1.0 - Docker Compose Configuration
# Phase 1: Placeholder for infrastructure services
# Services will be fully configured in Phase 2 (Infrastructure Setup)

version: '3.8'

services:
  # PostgreSQL with pgvector extension for vector storage
  # postgres:
  #   image: pgvector/pgvector:pg16
  #   container_name: enterprise-rag-postgres
  #   environment:
  #     POSTGRES_DB: enterprise_rag
  #     POSTGRES_USER: ${POSTGRES_USER}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - rag-network

  # Neo4j for knowledge graph storage
  # neo4j:
  #   image: neo4j:5.15-community
  #   container_name: enterprise-rag-neo4j
  #   environment:
  #     NEO4J_AUTH: neo4j/${NEO4J_PASSWORD}
  #     NEO4J_PLUGINS: '["apoc"]'
  #   ports:
  #     - "7474:7474"  # HTTP
  #     - "7687:7687"  # Bolt
  #   volumes:
  #     - neo4j_data:/data
  #     - neo4j_logs:/logs
  #   networks:
  #     - rag-network

  # Redis for caching and session management
  # redis:
  #   image: redis:7-alpine
  #   container_name: enterprise-rag-redis
  #   command: redis-server --requirepass ${REDIS_PASSWORD}
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   networks:
  #     - rag-network

# volumes:
#   postgres_data:
#   neo4j_data:
#   neo4j_logs:
#   redis_data:

# networks:
#   rag-network:
#     driver: bridge

# NOTE: Uncomment and configure these services in Phase 2
# Environment variables should be defined in .env file (not committed to Git)
